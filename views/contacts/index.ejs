<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
  </head>

  <style> div { position:relative; }
   #cm { position:absolute; }
    .hc { width:200px; left:0; right:0; margin-left:auto; margin-right:auto; } /* 가로 중앙 정렬 */
     .vc { height:40px; top: 0; bottom:0; margin-top:auto; margin-bottom:auto; } /* 세로 중앙 정렬 */
      .tc {width:60%; height: 100px; margin:auto; text-align: center;}
      </style>

  

  <body>
    <%- include('../partials/nav') %>

    <div>
      <h2 class="hc vc">설문 결과</h2><br>

      <h2><%=survey.survey%></h2>

      <table border="1" class="tc">
        <thead>
          <tr>
            <th>

            </th>
            <th>호랑이</th>
            <th>코끼리</th>
          </tr>
        </thead>
        <tfoot>
          <th>합</th>
          <th> <%= survey[0]+survey[2] %></th>
          <th> <%= survey[1]+survey[3] %></th>
        </tfoot>
        
          <tbody>
            <tr>
              <th>남</th>
              <th>
                <%= survey[0] %>
              </th>
              <th>
                <%= survey[1] %>
              </th>
            </tr>
            <tr>
              <th>여</th>
              <th>
                <%= survey[2] %>
              </th>
              <th>
                <%= survey[3] %>
              </th>
            </tr>         
          </tbody>
       
          <tr>
            <th>통계</th>
            <th>
              <% if(survey[0] > survey[1]) { %>
              <span>남자는 호랑이를 더 좋아합니다.</span>
              <% } else if (survey[0] < survey[1]) { %>
              <span>남자는 코끼리를 더 좋아합니다.</span>
              <% } else { %>
              <span>남자는 호랑이와 코끼리 둘다 좋아합니다.</span>
              <% } %>
            </th>
            <br>
            <th>
              <% if(survey[2] > survey[3]) { %>
              <span>여자는 호랑이를 더 좋아합니다.</span>
              <% } else if(survey[2] < survey[3]){ %>
              <span>여자는 코끼리를 더 좋아합니다.</span>
              <% } else { %>
              <span>여자는 호랑이와 코끼리 둘다 좋아합니다.</span>
              <% } %>
            </th>
          </tr>

      </table>
</div>
</body>
</html>
